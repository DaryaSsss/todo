/** function to summarise two numbers
 * @param { number } a - first number
 * @param { number } b - second number
 * @returns { number } - sum
 */
const sum = (a, b) => {
  const summ = a + b;
  return summ;
};

sum(5, 4);
